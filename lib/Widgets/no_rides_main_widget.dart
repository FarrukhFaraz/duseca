
import 'package:flutter/material.dart';
class NoRidesMainWidget extends StatelessWidget {
  const NoRidesMainWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('No Ride data'),);
  }
}
