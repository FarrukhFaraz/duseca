import 'package:flutter/material.dart';
class ReviewMainWidget extends StatelessWidget {
  const ReviewMainWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('No reviews'),);
  }
}
